findings:
  - rule_id:  DF004
    severity: HIGH
    file:     Dockerfile
    line:     3
    message: >
      ADD fetches a remote URL (https://example.com/install.sh). Remote
      content is unverified at build time; use COPY with a vetted local
      artifact, or curl plus checksum verification in a RUN step instead.
  - rule_id:  DF004
    severity: HIGH
    file:     Dockerfile
    line:     4
    message: >
      ADD fetches a remote URL (https://example.com/app-data.tar.gz). Remote
      content is unverified at build time; use COPY with a vetted local
      artifact, or curl plus checksum verification in a RUN step instead.
